<%- include('partials/_head'); %>
  <body>
    <input type="checkbox" hidden id="darkmodeHidden">
    <div class="main-container">
      <%- include('partials/_top-navbar'); %> 
      <%- include('partials/_menu_lateral'); %> 
      <main class="main">

        <%#--- Escuchados recientemente ----%>
        <section class="mt-4">
          <h2 class="mb-0 mb-lg-4">Escuchados recientemente</h2>
          <div class="row">
            <% artistas.forEach(artista => { %>
              <div class="col-lg-3 col-6 mt-4 mt-lg-0">
                <article class="categoria-musica-card shadow-card">
                  <a href="/artista/<%-artista.slug %>">
                    <div class="img-container">
                      <img class="img-fluid" src=" <%=artista.banner%>" load="lazy" alt="Imagen de <%=artista.nombre_artista%>">
                    </div>
                    <p class="titulo-resultado mb-0"> <%=artista.nombre_artista%></p>
                  </a>
                  <button data-play class="btn btn-play"> <i class="fas fa-play-circle"></i></button>
                </article>
              </div>
           <% }); %>
          </div>
        </section>

        <div class="row">
          <div class="col-lg-12">
            <hr class="separador">
          </div>
        </div>

        <%#--- Mis listas de reproduccion ----%>
        <section>
          <h2 class="mb-0 mb-lg-4">Mis listas de reprodución</h2>
          <div class="row">
            <% listaReproduccion.forEach(lista => { %>
              <div class="col-lg-3 col-6 mt-4 mt-lg-0">
                <article class="categoria-musica-card shadow-card">
                  <a href="/listas-reproduccion/<%=lista.slug %>">
                    <div class="img-container">
                      <img class="img-fluid" src="<%= lista.miniatura_lista %>" load="lazy" alt="Imagen de">
                    </div>
                    <p class="titulo-resultado mb-0"><%= lista.nombre_lista %></p>
                  </a>
                  <button data-play class="btn btn-play"> <i class="fas fa-play-circle"></i></button>
                </article>
              </div>      
            <% }) %>
          </div>
        </section>

        <div class="row">
          <div class="col-lg-12">
            <hr class="separador">
          </div>
        </div>
        
        <%#---Artistas Recomendados----%>
        <section style="margin-bottom: 4rem;">
          <h2 class=" mb-0 mb-lg-4">Artistas Recomendados</h2>
          <div class="row">
            <% artistasRecomendados.forEach(artista => { %>
              <div class="col-lg-3 col-6 mt-4 mt-lg-0">
                <article class="categoria-musica-card shadow-card">
                  <a href="/artista/<%= artista.slug %>">
                    <div class="img-container">
                      <img class="img-fluid" src="<%= artista.banner %>" load="lazy" alt="Imagen de <%= artista.nombre_artista %>">
                    </div>
                    <p class="titulo-resultado mb-0"> <%= artista.nombre_artista %> </p>
                  </a>
                  <button data-play class="btn btn-play"> <i class="fas fa-play-circle"></i></button>
                </article>
              </div>     
            <%})%>
          </div>
        </section>

      </main>
      <%- include('partials/_reproductor-bottom'); %> 
    </div>
<%- include('partials/_footer.ejs') %> 
